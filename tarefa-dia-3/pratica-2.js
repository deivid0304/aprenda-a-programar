//Tabuada de multiplicação de 1 a 10.//

let numeroBase = 9;
let contador = 1;
let multiplicador = 10;

while(contador <= multiplicador){
    console.log(`${numeroBase} x ${contador} = ${contador * numeroBase}`);
    contador++;
}